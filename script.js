console.log("hello guys");

console.log("second");

// git remote add origin URL    <= first step 
// git push origin commitName   <= second step

// git branch -D branchName             <= to delete the branch in my laptop
// git push origin --delete branchName  <= to delete the branch in git-hub
// git clone URL    <= to male clone of code from git-hub (fast way to download the code)